#include <stdio.h>;


float x, y, retorno;
void MAIN()
{
	//scanf("%t", x);
	//scanf("%t", y);
	retorno = AbsSub(x, y);
	//printf("%t", retorno);
	retorno = Factorial((	);
	//printf("%t", retorno);

}
float AbsSub(float num1, float num2)
{
	float num3;
	num3 = num1 - num2;
	if(num3 < 0)
		num3 = num3 * (0 - 1);
	return num3;
}
int Factorial(int n)
{
	if(n = 1)
		return 1;	return n * (	Factorial	(	));
}
- 1));

}
