PROGRAM errosem01
BEGIN

  FUNCTION INT main()
  BEGIN
  
    INT A;
    INT B;
    INT I;
    
    B := 1.5;
  
    READ(A);
    WRITE(A, B);
  
    FOR (I := 0; I < 3; I := I + 1)
	A := A + 1;
    ENDFOR
  
    IF (A > B) THEN
        A:=B;
    ENDIF
  
    RETURN 0;

  END

END
