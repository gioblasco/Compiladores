PROGRAM programa0

BEGIN

-- Escreve a soma entre dois números
  FUNCTION INT MAIN()
  BEGIN
  
  FLOAT A;
  INT B;

  A := 0;
  
  B := 1;
  
  READ(A);
  WRITE(A, B);
  
  FOR (I := 0; I < 3; I := I + 1)
	A := A + 1;
  ENDFOR
  
IF (A > B) THEN
	A:=B;
ENDIF
  
  RETURN 0;

  END

END
