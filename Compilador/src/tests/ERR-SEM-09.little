PROGRAM errosem09
BEGIN

  FUNCTION INT main(INT x, INT y)
  BEGIN
  
    INT A;
    INT B;
    INT I;
    
    B := 10 * 1.5 + 2;
  
    READ(A);
    WRITE(A, B);
  
    IF (A > B) THEN
        A:=B;
    ENDIF
  
    RETURN 0;

  END

END
