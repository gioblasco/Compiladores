PROGRAM errosem10
BEGIN

  INT A;

  FUNCTION INT MAIN()
  BEGIN
  
    INT A;
    INT B;
    INT I;
    FLOAT B;
    
    B := 10 * 1.5 + 2;
  
    READ(A);
    WRITE(A, B);
  
    IF (A > B) THEN
        A:=B;
    ENDIF
  
    RETURN 0;

  END

END
